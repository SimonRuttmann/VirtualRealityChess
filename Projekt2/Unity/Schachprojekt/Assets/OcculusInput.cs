using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Valve.VR;
//using Valve.VR;


public class OcculusInput : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
     //   if (SteamVR_Input._default.inAction.Teleport.GetStateDown(SteamVR_Input_Sources.Any))
    //    {

    //    }        
    }
}
